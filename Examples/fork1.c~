#include <stdio.h>
#include <unistd.h>

int main(int argc, char *argv[])
{
    int i;
    
    printf("Read to fork...\n");
    
    i = fork();
    
    printf("Fork returned %d\n", i);

    while (1);
    
    return 0;
}

